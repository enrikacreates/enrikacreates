/**
 * Schema registry.
 *
 * MIGRATION TEMPLATE NOTE:
 *   Every document type defined in /sanity/schemas/ must be exported here.
 *   Phase 1 ships an empty registry — schemas land in Phase 2.
 */

import type { SchemaTypeDefinition } from "sanity";

export const schemaTypes: SchemaTypeDefinition[] = [
  // Phase 2 adds: project, blogPost, siteSettings, etc.
];
